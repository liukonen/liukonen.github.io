{
  "name": "my-spa",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && shx rm -f ../index.html && shx rm -rf ../assets && shx cp -r dist/* ../"
  },
  "dependencies": {
    "@gitgraph/js": "^1.4.0",
    "preact": "^10.26.9"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "sass": "^1.93.2",
    "shx": "^0.4.0",
    "typescript": "^6.0.0",
    "vite": "^8.0.5"
  }
}
