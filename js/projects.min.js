const innerBind=(template,dataArray,elementName)=>document.getElementById(elementName).insertAdjacentHTML("beforeend",dataArray.map(item=>template(item)).join("")),innerhtml=(template,dataArray)=>dataArray.map(item=>template(item)).join(""),projectTemplate=d=>`<div v-for="product in this.Products">
    <div class="col mb-4 hoverItem">
        <div class="card shadow">
            <div class="">
                <img src="${d.image}" loading="lazy" class="card-img-top himg" alt="${d.Title}" />
            </div>
            <div class="card-body">
                <h5 class="card-title">${d.Title}</h5>
                <p class="card-text">${d.SubTitle}<button class="btn btn-link text-dark" onclick="ExpandText(this)">[+]</button></p>
                <div class="content">${d.Description}</div>
                <p>
                ${generateButtons(d.buttons)}
                <p>Learnings</p>
                <div>
                ${generateLearnings(d.tags)}
                </div>
            </div>
        </div>
    </div>
</div>
`,buttonTemplate=d=>`<a class="${DetermButtonColor(d.type)}" href="${d.url}" target="_blank">${d.type}</a>`,learningsTemplate=d=>`<span class="badge text-dark">${d}</span> `;function generateButtons(items){return innerhtml(buttonTemplate,items)}function generateLearnings(items){return innerhtml(learningsTemplate,items)}function loadProjects(){getFile("./json/page.json").then(sourceData=>{innerBind(projectTemplate,sourceData.projects,"products")})}function DetermButtonColor(ItemType){return"website"==ItemType.toLowerCase()?"btn btn-success shadow ml-2":"release"==ItemType.toLowerCase()?"btn btn-primary shadow ml-2":"btn btn-secondary shadow ml-2"}function ExpandText(me){var content=me.parentElement.nextElementSibling,isContentVisible="block"===content.style.display;me.innerHTML=isContentVisible?"[+]":"[-]",content.style.display=isContentVisible?"none":"block"}function getFile(fileUrl){return fetch(fileUrl).then(response=>{if(response.ok)return response.json();throw new Error(response.status+" "+response.statusText)}).catch(error=>{window.alert(error)})}loadProjects(),document.getElementById("year").innerHTML=(new Date).getFullYear();